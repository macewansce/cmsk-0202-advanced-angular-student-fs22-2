import { RedFlagDirective } from './red-flag.directive';

describe('RedFlagDirective', () => {
  it('should create an instance', () => {
    const directive = new RedFlagDirective();
    expect(directive).toBeTruthy();
  });
});
